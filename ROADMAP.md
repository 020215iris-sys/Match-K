# Mātch K — 공모전 로드맵 (1차 심사 대비)

> 기준일 2026-07-31. 1차 심사 제출 **8/10(월)~9/21(월) 16:00**, 개발 목표 **8월 말**.
> 역할·파일 상세는 `HANDOFF.md`·`DESIGN.md`, 환경은 `TEAM_SETUP.md` 참고.
> **깃대장: 다은** (브랜치 보호·머지 관리·CI 게이트).

---

## 1. 날짜별 일정 (마감 역산)

| 구간 | 기간 | 목표 |
|---|---|---|
| **Phase 0 · 공용 뼈대 확정** | ~8/3 | 개편 뼈대 PR 머지, 공용 계약 고정 |
| **Phase 1 · 핵심 실동작화** | 8/4~8/17 | 각자 영역 "진짜 도는 화면"으로 (2주) |
| **Phase 2 · 스토어 등록 착수 + 통합** | 8/18~8/24 | EAS 빌드 → 원스토어 제출, 실기기 통합 |
| **Phase 3 · 기능 완성·버그·스토어 승인** | 8/25~9/7 | 개발 8월 말 목표, 스토어 반려 대응 버퍼 |
| **Phase 4 · 제출물 작성·제출** | 9/8~9/18 | 기능설명서·이미지·인증키 정리, 제출 완료 |
| **버퍼** | 9/19~9/21 16:00 | 예비 (마감 여유) |

### ⏳ 지금 당장 착수 (대기시간 있는 항목 — 늦으면 병목)
- **TourAPI 운영키 신청** (전원, 개발기간 호출이력이 심사 근거)
- **구글 OAuth 클라이언트**·**Papago 키** 발급 (정현·지현)
- **스토어 개발자 계정** 등록 + **개인정보처리방침 URL** 게시 (정현)
- **부산 지도 이미지 제작** 의뢰 (전체 + 구별 — 현표/디자인)

---

## 2. 겹치는 영역을 고려한 개발 순서

> 원칙: **공용 계약을 먼저 못박고, 각자 자기 소유 파일에서 병렬.** 접점은 파라미터/응답 형태만 합의하면 서로 안 기다려도 된다.

1. **(Phase 0, 다은=깃대장) 공용 파일 단독 PR 먼저** — `RootNavigator`·`endpoints.ts`·`client.ts`·모델·`main.py`·`core`. 이게 머지돼야 나머지가 안 충돌.
2. **(Phase 1 첫날, 지현↔다은) 검색상세 ↔ 스케줄러 왕복 계약 고정** — 코드의 `PlacePayload`/`pick` 파라미터 형태를 "이걸로 간다" 합의 후 각자 진행.
3. **(Phase 1 첫주 회의, 새봄↔현표) 히든 대상 선정 기준** — 어떤 장소를 히든으로(새봄 coverage 기준) + 팝업 표시(현표).
4. **(Phase 1, 현표↔지현) 업적 3페이지 도장 리스트** — `landmarksByDistrict`(지현) + `districtStampStatus`(현표) 조합. 응답형태만 맞추면 병렬.
5. **나머지는 각자 소유 파일에서 독립 병렬** — 지현(검색 LLM·번역), 다은(스케줄러 캘린더·DnD), 현표(지도 이미지·GPS 도장·히든 팝업), 정현(OAuth·배포·스토어), 새봄(역추천 튜닝).

> 다은(깃대장)이 머지 게이트를 잡고, 공용 파일은 항상 단독 PR 먼저 규칙만 지키면 주 1회 머지에도 충돌이 거의 없다.

---

## 3. 남는 시간에 하면 좋은 것들 (완성도·편의성 점수 + 심사 이미지 소재)

우선순위 순. 급하진 않지만 **심사 완성도 점수와 제출용 스크린샷에 직접** 먹힌다.

1. **프로필 화면 보강** — 지금 시각적으로 비어 보임. 마스코트/아바타, **내 도장 요약**(N개·구 몇 곳 색칠), 언어 버튼 국기 이모지, 앱 버전·문의, (데모 토글).
2. **도장 획득 애니메이션 + 진동**, 마스코트 표정 전환 — 시연 임팩트 큼.
3. **빈 상태(Empty state) 문구·일러스트** — 일정 없음/검색 전/도장 0개 (지금은 "—"뿐).
4. **로딩 스켈레톤 + 에러 재시도 UI 통일**, 온보딩 인트로 다듬기.
5. **대표/상세 스크린샷용 화면 미화** — 기능설명서 대표 1장 + 상세 3~5장에 그대로 반영.
6. i18n 문구 원어민 톤 감수, 접근성(글자크기), (선택) 다크모드.
7. **시연 시나리오·데모 계정 문서** — 테스트 계정(게스트 로그인 → "로그인 불필요"로 체크 가능) + 동선 정리.

---

## 4. 1차 심사 제출 요건 체크리스트 (누락 시 심사 제외)

- [ ] **앱 스토어 등록 링크** (원스토어 등 최소 1개) — ★최대 리스크, 심사기간 추가 없음. 8월 중 등록 시작.
- [ ] **테스트 계정** — 게스트 로그인 활용 → "로그인 불필요" 또는 구글 "SNS 연동" 체크 (별도계정 방식이면 `openapi / 2026openapi!` 지정형식 준수).
- [ ] **한국관광공사 OpenAPI 인증키**(인코딩·디코딩) + 개발기간 **실호출 이력** (대표 1개 운영계정은 선택).
- [ ] **기능설명서**(지정 양식, PDF) — 서비스 개요/주제 선정 이유/**핵심기능 최대 5개 + 흐름도**(`DESIGN.md` 사용자 흐름도 재활용)/대표이미지 1 + 상세 3~5장/차별성·발전계획.
- [ ] **최종 팀원 전원 회원가입** (팀 대표 변경 불가).
- [ ] 지역특화(부산) 체크 → **가점 +2**. (해당 시 Start-up NEST +2)

### 심사 기준 대비 우리 위치 (요약)
- 기획력 30 — **강함**(언어권별 역추천·소멸위험 구·게임화)
- 완성도 30 — **승부처**(실동작 화면 + 스토어 등록)
- 데이터 활용 20 — **유리**(TourAPI 실시간 호출 = 호출이력 검증에 정렬)
- 발전성 20 — **서사 좋음**(일정 데이터로 역추천 보강 루프)
