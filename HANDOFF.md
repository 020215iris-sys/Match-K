# Mātch K — 프로젝트 개요 & 개발 가이드

> 팀원 온보딩용. 이 서비스가 뭐고, 지금 어디까지 됐고, 어떤 기술을 왜 쓰며, 앞으로 뭘 개발하는지.
> (환경 세팅·협업 규칙은 `TEAM_SETUP.md` 참고)

---

## 1. 서비스 한 줄 소개

부산 인바운드 외국인을 위한 **관광 추천 + 도장(업적) 앱**.
핵심 차별점 두 가지:

- **언어권별 역추천** — "내 언어권(영/일/중)엔 아직 안 알려진 부산의 로컬 스팟"을 골라 추천. 국문 관광정보는 많지만 외국어는 적다는 실제 데이터 격차(국문 155 vs 외국어 ~50)를 신호로 씀.
- **소멸위험 지역 재발견** — 발길 끊긴 구(동구·서구·영도구)를 게임처럼 방문 유도. 공모전 주최 취지(인구소멸지역 살리기)와 정렬.

> 서사: **"외국어 정보가 없는 = 소외된 지역"**을 언어 데이터로 발견 → 도장·히든미션으로 방문 유도.

---

## 2. 기술 스택 (무엇을 왜)

### 백엔드 `matchk-api`

| 기술 | 왜 |
|---|---|
| **FastAPI** (Python 3.10) | 비동기 API 서버. TourAPI를 실시간 병렬 호출해야 해서 async 지원이 중요. 자동 문서(`/docs`) 제공. |
| **SQLAlchemy + Alembic** | ORM + 마이그레이션. 파이썬 객체로 DB 다루고 스키마 변경 관리. |
| **SQLite (개발) → PostgreSQL (배포)** | 개발은 파일 하나로 끝나는 SQLite라 설치 불필요. 배포는 동시접속·안정성 위해 PostgreSQL. SQLAlchemy 덕에 코드 변경 없이 전환. |
| **httpx** | TourAPI·Papago 비동기 HTTP 호출. |
| **cachetools** | TTL 캐시(5분/24시간). TourAPI 일 1,000건 한도 보호 + 응답 속도. |
| **PyJWT** | 로그인 토큰. 게스트/구글 사용자 인증. |

### 프론트 `matchk-app`

| 기술 | 왜 |
|---|---|
| **Expo (React Native)** | 코드 하나로 iOS·안드로이드 둘 다. 웹 개발 경험 살리기 좋고, 빌드·배포가 EAS로 간단. |
| **React Navigation** | 화면 전환(스택). |
| **i18next** | 4개 언어(ko/en/ja/zh) 자동 감지 + 수동 전환. |
| **zustand** | 가벼운 전역 상태(로그인·언어·위치). |
| **expo-location / secure-store / auth-session** | GPS·토큰 저장·구글 로그인. |
| **react-native-webview** | (스케줄러의 카카오 지도용) |

### 인프라

| 기술 | 왜 |
|---|---|
| **Railway** | 서버+PostgreSQL 배포. Dockerfile 자동 인식, 무료 크레딧으로 공모전 기간 커버. |
| **GitHub Actions (CI)** | PR마다 백엔드 테스트·프론트 타입체크 자동 실행 → 깨진 코드 머지 차단. |
| **Papago (네이버클라우드)** | 외국어판 없는 곳의 폴백 번역. |

### 왜 "폰 자체 DB"가 아니라 서버를 쓰나
관광 데이터가 **한국관광공사 TourAPI**에서 오고, 이 API는 서비스 키로 **서버에서** 호출해야 함(키 보안 + 공모전의 실시간 호출 검증 조건). 도장 기록·계정도 여러 기기에서 유지되려면 서버 저장이 필요. → 앱은 화면, 서버는 데이터·로직. (인스타·배민과 같은 구조)

---

## 3. 현재 구성 (어디까지 됐나)

화면 8개, 백엔드 엔드포인트 다수가 이미 작동. 상태: ✅ 작동 / 🔶 뼈대(동작은 하나 개선/교체 필요) / ⬜ 없음

| 기능 | 상태 | 설명 |
|---|---|---|
| 홈(추천 카드) | ✅ | GPS 기반 역추천, 인트로 팝업 |
| 검색 | ✅ | 실시간 TourAPI 검색(디바운싱) |
| 상세 | ✅ | 사진·설명·도장. 언어 자동교체 + 번역 폴백(키 대기) |
| 구별 리스트 | ✅ | 구 선택 시 관광지 목록 |
| 도장 찍기 | ✅ | GPS 거리검증(**폰 내 계산** — 위치 서버전송 X) |
| 업적지도 | 🔶 | 진행률·색칠 데이터는 작동. 지금 카카오 WebView → **이미지 기반으로 교체 예정** |
| 스케줄러 | 🔶 | "검색→연관목록"까지 됨. **GPS 핀 지도로 재구성 예정** |
| 프로필 | ✅ | 언어 4개 전환, 로그아웃 |
| 히든 미션 | ✅(백엔드) | 조우 팝업·별도 컬렉션·잠금조건 완성. 디자인·연출 남음 |
| 로그인 | 🔶 | 게스트 ✅ / 구글 OAuth는 키·프론트 연결 남음 |
| 번역 | 🔶 | 유틸·연결 완성. **Papago 키만 넣으면 작동** |

### 역추천 로직 (핵심 엔진, `recommender.py`)
1. 후보 수집(국문 관광지) → 2. 방문자 적은 곳 가점 → 3. 내 언어권 미등록 가점 → 4. 소멸위험 구 쿼터(최소 60%) → 5. 표시를 사용자 언어로 교체(없으면 번역).

### 히든 미션 (`hidden.py`, 포켓몬 고식)
일반 도장판 **밖의** 별도 장소(소멸위험 구 + 외국어 미등록). 지도 20%+스탬프 30% 달성 시 잠금 해제 → 근처 지나가면 "숨은 곳 발견!" 팝업 → 별도 컬렉션에 수집. 총 개수 비공개. GPS는 폰 안에서만 계산.

---

## 4. 앞으로 개발 방향 (담당별)

| 담당 | 할 일 |
|---|---|
| **새봄** 역추천 | coverage 기준으로 히든 지정 검토, 발동 비율·가중치 튜닝, 신규 API(관광다양성·SNS) 실사 |
| **현표** 업적·도장 | 업적지도 **이미지 기반 교체**, 도장·히든 조우 애니메이션/진동, 히든 컬렉션 화면 |
| **지현** 상세·검색·번역 | Papago 키 발급, 번역 품질 보정, 검색 다듬기 |
| **다은** 스케줄러 | **GPS 핀 지도로 재구성**(nearby API 재사용), 카카오 지도 핀+정보카드 |
| **정현** 인증·언어 + 깃대장 | 구글 OAuth 프론트 연결, 개인정보처리방침, 배포·스토어·CI 운영 |

### 미정(회의 안건)
- 히든 발동 비율 정확한 수치 (현재 임시 20%/30%)
- 업적지도 이미지 소스(부산 지도 일러스트)
- 신규 API 2종 채택 여부

---

## 5. 반드시 기억할 제약 3가지

1. **API는 실시간 호출** — TourAPI를 로컬 DB에 통째로 저장하면 심사 불이익. 참조 키(좌표·구)만 DB, 콘텐츠는 실시간. 개발 기간 호출 이력이 심사 근거.
2. **위치는 서버로 안 보냄** — 도장·히든의 거리 계산은 폰 안에서. 위치정보사업자 신고 회피.
3. **앱은 스토어 출시 필수** — 원스토어(심사 빠름) 최소 1개. 9/21 마감 역산해서 준비. 서버 URL·개인정보처리방침 필요.

---

## 6. 빠른 확인

```bash
# 백엔드 (21개 테스트 통과 상태)
cd matchk-api && pytest
# 프론트 타입체크
cd matchk-app && npm run typecheck
```

DB 스키마·환경 세팅·협업 규칙은 **TEAM_SETUP.md** 참고.
